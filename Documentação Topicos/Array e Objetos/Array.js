/*
const numeros = [34, 44, 21, 10];

var cont = 1;

for(var i = 0; i <= numeros.length; i++){
    const par = numeros[i];
    if(par % 2 ===0){
        console.log(par);
    }else{
        console.log('Não é um número par!');
    }
};
*/

const palavras = ['Parar', 'o', 'carro em um cruzamento', 'é proibido.']

console.log(palavras.join());